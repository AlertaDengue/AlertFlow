[tool.poetry]
name = "alertflow"
version = "1.1.0"  # changed by semantic-release
description = "Airflow for AlertaDengue"
authors = ["Luã Bida Vacaro <luabidaa@gmail.com>"]
license = "GNU"
readme = "README.md"
packages = [{include='alertflow'}]

[tool.poetry.dependencies]
python = ">= 3.10, <4"
satellite-weather-downloader = ">=1.7.0"


[tool.poetry.group.pre-commit.dependencies]
pre-commit = "^3.0.4"
pre-commit-hooks = "^4.4.0"
blue = "^0.9.1"
flake8 = ">=3.8"
isort = "^5.12.0"


[tool.poetry.group.dev.dependencies]
ipython = "^8.11.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
